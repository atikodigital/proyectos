# K.A.L.Y. F1-B — App (esfera, Gemini Live, orquestador, 4 botones)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Spec: `docs/superpowers/specs/2026-06-12-kaly-fase1-agente-voz-design.md`. Backend F1-A ya implementado (endpoints `/api/app/agent/session`, `/agent/prefs`, `/expenses/:id/pagar`, `/agent/resumen-whatsapp`, 202 documento).

**Goal:** K.A.L.Y. hablando en vivo dentro de Hash IA: esfera dorada arriba de los botones de captura, onboarding/saludo/timeouts, acciones por voz, barra de 4 botones y manejo del 202 cartola/libro.

**Tech Stack:** React 18 (gastos-app), WebSocket Gemini Live v1alpha (token efímero), Web Audio (PCM 16k in / 24k out), jest+RTL (WS/audio mockeados).

**Convenciones:** tests en `gastos-app/tests/gastos/`; correr `cd gastos-app && npx jest`. Código nuevo en `gastos-app/src/gastos/kaly/`. El JSON del usuario (system prompt K.A.L.Y.) está embebido en el spec — usarlo FIEL.

---

## Task 1: `kaly/prompt.js` + `kaly/logic.js` + `api.js` (cliente)

**Create** `gastos-app/src/gastos/kaly/prompt.js`:
- Exporta `buildSystemPrompt(context)` (context = el del backend: nombre/trato/onboarded/saludoHora/empresaNombre/resumen) y `instruccionInicial(context, motivo)` con motivo ∈ `'onboarding'|'saludo'|'inactividad'|'manual'`.
- El prompt en español, FIEL al JSON del usuario: identidad ("Eres K.A.L.Y., agente de IA especializado en asistencia contable de la app Hash IA"), tono profesional/proactivo/amable/eficiente, trata SIEMPRE de "[trato]" (o pregunta el trato si no lo sabe), respuestas CONCISAS (1-3 frases), español.
- Incluye: datos del contexto (empresa, resumen del mes con cifras CLP, pendientes de pago); guion de onboarding (presentarse "Soy K.A.L.Y., su asistente contable…", preguntar nombre y trato → llamar tool `guardar_preferencias`, explicar los 4 botones con el guion del JSON: Captura/Movimientos/Transaccional próximamente/Match rojo próximamente con conciliación SII+cartolas e iteración de sumas); regla de CIERRE: si el usuario dice no/nada/gracias → despedida de UNA frase; regla dura: NUNCA ejecutar `marcar_pagada`/`anular_movimiento`/`enviar_resumen_whatsapp` sin confirmación verbal explícita del usuario en el turno inmediatamente anterior (preguntar "¿Confirma, [trato]?").
- `instruccionInicial`: onboarding → "Realiza el onboarding completo ahora."; saludo → "Saluda según la hora ([saludoHora]) usando el nombre y trato guardados y pregunta: ¿en qué trabajaremos hoy? ¿Necesita ayuda?"; inactividad → "Pregunta brevemente si puedes ayudar en algo."; manual → "El usuario abrió la conversación tocando la esfera; salúdalo brevemente y queda a la orden."

**Create** `gastos-app/src/gastos/kaly/logic.js` (puro, testeable):

```js
export const SILENCE_MS = 5000;
export const INACTIVITY_MS = 5 * 60 * 1000;
export function hoyStr(d = new Date()) { return d.toISOString().slice(0, 10); }
export function decideAutoStart({ onboarded, lastGreet, today }) {
  if (!onboarded) return 'onboarding';
  if (lastGreet !== today) return 'saludo';
  return null;
}
export function esNegativa(texto) {
  const t = String(texto || '').toLowerCase().trim();
  return /^(no|nada|no gracias|gracias|estoy bien|ninguna|nada m[aá]s|eso es todo|listo gracias)[.,!\s]*$/.test(t);
}
```

**Modify** `gastos-app/src/gastos/api.js` — agregar al objeto `api`:

```js
  agentSession() { return req('/api/app/agent/session', { method: 'POST', body: {} }); },
  agentPrefs(patch) { return req('/api/app/agent/prefs', { method: 'PATCH', body: patch }); },
  pagarExpense(id) { return req(`/api/app/expenses/${id}/pagar`, { method: 'PATCH' }); },
  resumenWhatsapp() { return req('/api/app/agent/resumen-whatsapp', { method: 'POST', body: {} }); },
```

**Test** `gastos-app/tests/gastos/kaly-logic.test.js`: decideAutoStart (onboarding si !onboarded; saludo si lastGreet≠today; null si ya saludó), esNegativa ('no'/'no gracias'/'eso es todo' true; 'sí'/'cuánto llevo' false), buildSystemPrompt contiene 'K.A.L.Y.', el trato, las cifras del resumen y la regla de confirmación; instruccionInicial('onboarding') menciona onboarding. Test api: los 4 métodos nuevos pegan al endpoint correcto (fetch mockeado, patrón de api.test.js).

**Commit:** `feat(kaly): prompt fiel al JSON, logica de arranque/cierre y cliente api`

---

## Task 2: `kaly/live.js` (cliente Gemini Live) + `kaly/tools.js`

**Create** `gastos-app/src/gastos/kaly/tools.js`:

```js
import { api } from '../api';

export const TOOL_DECLARATIONS = [
  { name: 'guardar_preferencias', description: 'Guarda nombre y trato preferido del usuario en memoria permanente', parameters: { type: 'OBJECT', properties: { nombre: { type: 'STRING' }, trato: { type: 'STRING', description: 'señor o señora' } } } },
  { name: 'obtener_resumen', description: 'Resumen del mes: ingresos, gastos, saldo', parameters: { type: 'OBJECT', properties: {} } },
  { name: 'listar_movimientos', description: 'Lista los últimos movimientos', parameters: { type: 'OBJECT', properties: { limite: { type: 'NUMBER' } } } },
  { name: 'marcar_pagada', description: 'Marca como pagado un gasto YA CONFIRMADO VERBALMENTE por el usuario', parameters: { type: 'OBJECT', properties: { proveedor: { type: 'STRING', description: 'proveedor o descripción del gasto' } } } },
  { name: 'anular_movimiento', description: 'Anula un movimiento YA CONFIRMADO VERBALMENTE por el usuario', parameters: { type: 'OBJECT', properties: { proveedor: { type: 'STRING' } } } },
  { name: 'enviar_resumen_whatsapp', description: 'Envía el resumen de flujo de caja al WhatsApp del dueño (requiere confirmación verbal previa)', parameters: { type: 'OBJECT', properties: {} } },
];

function buscarMovimiento(rows, proveedor) {
  const q = String(proveedor || '').toLowerCase();
  return rows.find((r) => String(r.proveedor || '').toLowerCase().includes(q)) || null;
}

export async function executeTool(name, args = {}, { onPrefsSaved } = {}) {
  try {
    if (name === 'guardar_preferencias') {
      await api.agentPrefs({ nombre: args.nombre, trato: args.trato, onboarded: true });
      if (onPrefsSaved) onPrefsSaved(args);
      return { ok: true };
    }
    if (name === 'obtener_resumen' || name === 'listar_movimientos') {
      const rows = await api.listExpenses();
      if (name === 'listar_movimientos') return { movimientos: rows.slice(0, args.limite || 5).map((r) => ({ tipo: r.tipo, proveedor: r.proveedor, total: r.total, estado: r.estado, estado_pago: r.estado_pago })) };
      let gastos = 0; let ingresos = 0;
      for (const r of rows) { if (r.estado !== 'confirmado') continue; if (r.tipo === 'ingreso') ingresos += Number(r.total) || 0; else gastos += Number(r.total) || 0; }
      return { ingresos, gastos, saldo: ingresos - gastos };
    }
    if (name === 'marcar_pagada' || name === 'anular_movimiento') {
      const rows = await api.listExpenses();
      const mov = buscarMovimiento(rows, args.proveedor);
      if (!mov) return { error: 'no_encontrado', detalle: 'No encontré un movimiento que coincida.' };
      if (name === 'marcar_pagada') { const r = await api.pagarExpense(mov.id); return { ok: true, proveedor: mov.proveedor, total: mov.total, estado_pago: r.estado_pago }; }
      await api.annulExpense(mov.id);
      return { ok: true, anulado: mov.proveedor, total: mov.total };
    }
    if (name === 'enviar_resumen_whatsapp') { const r = await api.resumenWhatsapp(); return { ok: true, enviado_a: r.to }; }
    return { error: 'tool_desconocida' };
  } catch (e) {
    return { error: 'fallo_operacion', detalle: e.message || 'error' };
  }
}
```

**Create** `gastos-app/src/gastos/kaly/live.js` — cliente WS sin React. Contrato:

```js
export function openLiveSession({ token, model, systemPrompt, tools, onAudioLevel, onState, onUserTranscript, onToolCall, onClose, wsFactory, audio = true })
// → { sendText(text), sendToolResponse(id, name, response), close() }
```

Implementación (completa; `wsFactory` y `audio:false` permiten testear el protocolo sin navegador):

```js
const WS_HOST = 'wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1alpha.GenerativeService.BidiGenerateContent';

export function openLiveSession(opts) {
  const { token, model, systemPrompt, tools, onAudioLevel, onState, onUserTranscript, onToolCall, onClose, wsFactory, audio = true } = opts;
  const ws = (wsFactory || ((url) => new WebSocket(url)))(`${WS_HOST}?access_token=${encodeURIComponent(token)}`);
  let closed = false; let micStop = null; let player = null;

  const send = (obj) => { try { ws.send(JSON.stringify(obj)); } catch (e) {} };
  const setState = (s) => { if (onState) onState(s); };

  ws.onopen = () => {
    send({ setup: {
      model: `models/${model}`,
      generationConfig: { responseModalities: ['AUDIO'], speechConfig: { voiceConfig: { prebuiltVoiceConfig: { voiceName: 'Charon' } }, languageCode: 'es-US' } },
      systemInstruction: { parts: [{ text: systemPrompt }] },
      tools: [{ functionDeclarations: tools }],
      inputAudioTranscription: {},
    } });
  };

  ws.onmessage = async (ev) => {
    let data = ev.data;
    if (data instanceof Blob) data = await data.text();
    let msg; try { msg = JSON.parse(data); } catch (e) { return; }
    if (msg.setupComplete) { setState('live'); if (audio) micStop = await startMic(send, onAudioLevel).catch(() => null); if (audio) player = createPlayer(onAudioLevel, setState); return; }
    if (msg.toolCall && msg.toolCall.functionCalls) { for (const fc of msg.toolCall.functionCalls) onToolCall && onToolCall(fc); return; }
    const sc = msg.serverContent;
    if (!sc) return;
    if (sc.inputTranscription && sc.inputTranscription.text) onUserTranscript && onUserTranscript(sc.inputTranscription.text);
    if (sc.interrupted) { if (player) player.flush(); setState('listening'); }
    if (sc.modelTurn && sc.modelTurn.parts) {
      for (const p of sc.modelTurn.parts) {
        if (p.inlineData && p.inlineData.data) { setState('speaking'); if (player) player.push(p.inlineData.data); }
      }
    }
    if (sc.turnComplete) { if (player) player.onDrain(() => setState('listening')); else setState('listening'); }
  };

  ws.onerror = () => setState('error');
  ws.onclose = () => { cleanup(); onClose && onClose(); };

  function cleanup() { if (closed) return; closed = true; if (micStop) micStop(); if (player) player.stop(); }

  return {
    sendText(text) { send({ clientContent: { turns: [{ role: 'user', parts: [{ text }] }], turnComplete: true } }); },
    sendToolResponse(id, name, response) { send({ toolResponse: { functionResponses: [{ id, name, response }] } }); },
    close() { cleanup(); try { ws.close(); } catch (e) {} },
  };
}
```

Helpers de audio (mismo archivo): `startMic(send, onLevel)` → getUserMedia mono → AudioContext({sampleRate:16000}) → ScriptProcessor(4096,1,1) → Float32→Int16→base64 → `send({ realtimeInput: { audio: { data, mimeType: 'audio/pcm;rate=16000' } } })`; calcula RMS → onLevel('in', v). Devuelve función stop (tracks+ctx). `createPlayer(onLevel, setState)` → AudioContext({sampleRate:24000}); `push(b64)` decodifica Int16→Float32, crea AudioBuffer y lo agenda secuencial (cursor de tiempo); RMS → onLevel('out', v); `flush()` para interrupción (resetea cola), `onDrain(cb)` dispara cuando termina lo agendado, `stop()` cierra. Conversión base64↔Int16 con `atob`/loop (sin Buffer).

**Test** `gastos-app/tests/gastos/kaly-live.test.js` (protocolo, `audio:false`, FakeWS clase con send/close + capacidad de simular mensajes): setup enviado al abrir (model, Charon, systemInstruction, tools, inputAudioTranscription); setupComplete → onState('live'); toolCall → onToolCall con functionCall; inputTranscription → onUserTranscript; sendToolResponse manda functionResponses; close() cierra y dispara onClose una vez.
**Test** `gastos-app/tests/gastos/kaly-tools.test.js`: executeTool de cada tool pega al método correcto de api (api mockeado), marcar_pagada busca por proveedor (lista mockeada) y llama pagarExpense con el id correcto; tool desconocida → error; api throw → {error:'fallo_operacion'}.

**Commit:** `feat(kaly): cliente Gemini Live (WS+audio PCM) y tools por voz`

---

## Task 3: `kaly/KalyOrb.jsx` + `kaly/KalyAgent.jsx`

**Create** `KalyOrb.jsx`: adaptar `JarvisCore` de `gastos-app/J.A.R.V.I.S..html` (líneas 136-249): mismo SVG reactivo pero (a) fondo TRANSPARENTE (sin stage/glass; solo el SVG + halos blur), (b) tamaño 170×170 viewBox 0 0 300 300, (c) colores por estado: idle `#C9A24B` opacidad .45, connecting ámbar `#d8b25e`, live/listening `#C9A24B` brillante, speaking `#E6C36A` con nivel, error `#ff5a5a`, (d) sin textos SYS.CORE (poner "K.A.L.Y." pequeño bajo el núcleo, font-black, dorado), (e) `onClick={onTap}` en el contenedor (cursor-pointer, role="button", aria-label="K.A.L.Y."), props `{ state, audioLevel, onTap }`. Mantener wavePath/arcos/anillos con `requestAnimationFrame` (en jsdom rAF existe; el test solo monta y verifica estados→color base vía data-attr `data-state={state}` en el contenedor).

**Create** `KalyAgent.jsx` (orquestador):

```jsx
import { useEffect, useRef, useState } from 'react';
import { api } from '../api';
import KalyOrb from './KalyOrb.jsx';
import { openLiveSession } from './live.js';
import { TOOL_DECLARATIONS, executeTool } from './tools.js';
import { buildSystemPrompt, instruccionInicial } from './prompt.js';
import { decideAutoStart, esNegativa, hoyStr, SILENCE_MS, INACTIVITY_MS } from './logic.js';
```

Estado: `state` ('off'|'connecting'|'live'|'listening'|'speaking'|'error'), `level`. Refs: `sessionRef`, `silenceTimerRef`, `inactivityTimerRef`, `contextRef`.
- `start(motivo)`: si sessionRef.current → return. setState('connecting'). `const s = await api.agentSession()` (catch → 'error' 3 s → 'off'). `contextRef.current = s.context`. `sessionRef.current = openLiveSession({ token: s.token, model: s.model || __KALY_LIVE_MODEL__, systemPrompt: buildSystemPrompt(s.context), tools: TOOL_DECLARATIONS, audio: true, onState: setStateAndTimers, onAudioLevel, onUserTranscript, onToolCall, onClose: () => { sessionRef.current = null; setState('off'); } })`. Tras abrir: `sessionRef.current.sendText(instruccionInicial(s.context, motivo))`. Si motivo==='saludo' → `localStorage.setItem('kaly_last_greet', hoyStr())`.
- `stop()`: close session, clear silence timer, state 'off'.
- **Silencio:** cuando onState pasa a 'listening' (KALY terminó de hablar) → arma timer SILENCE_MS → al vencer `stop()`. Cuando llega `onUserTranscript` → limpia el timer; si `esNegativa(texto)` → tras 2.5 s (deja despedirse) `stop()`.
- **onToolCall:** `const out = await executeTool(fc.name, fc.args, { onPrefsSaved: (p) => { contextRef.current = { ...contextRef.current, ...p, onboarded: true }; } }); sessionRef.current && sessionRef.current.sendToolResponse(fc.id, fc.name, out);`
- **Auto-start (mount):** `useEffect` una vez: `api.agentSession` NO se llama aún; primero decide con datos locales → necesita onboarded del backend, así que: `const motivo = await (async () => { try { const s = await api.agentSession(); contextRef.current = s.context; preSessionRef.current = s; return decideAutoStart({ onboarded: s.context.onboarded, lastGreet: localStorage.getItem('kaly_last_greet'), today: hoyStr() }); } catch { return null; } })();` — para no quemar el token: MEJOR: el auto-start llama `start(motivo)` que pide SU PROPIA sesión; para decidir el motivo sin token agregar en api `agentContext()`… **Simplificación adoptada:** `decideAutoStart` se evalúa con localStorage: `kaly_onboarded` ('1' si ya se hizo; se setea cuando guardar_preferencias corre o cuando el context llega con onboarded=true) y `kaly_last_greet`. Si decide motivo ≠ null → `start(motivo)` (la sesión trae el context real; si context.onboarded=true y motivo era 'onboarding', KALY igual recibe instrucción de saludo — corregir motivo tras recibir context: `if (motivo==='onboarding' && s.context.onboarded) motivo='saludo'`; y setear `localStorage.kaly_onboarded='1'`).
- **Inactividad:** useEffect global: listeners `touchstart`/`click` en window resetean `inactivityTimerRef` (INACTIVITY_MS). Al vencer y `state==='off'` → `start('inactividad')`. Se re-arma después.
- Render: `<div className="flex flex-col items-center pt-2 pb-1"><KalyOrb state={state} audioLevel={level} onTap={() => (state === 'off' ? start('manual') : stop())} />{state==='error' ? <p className="text-xs text-red-400">K.A.L.Y. no disponible</p> : null}</div>`

`__KALY_LIVE_MODEL__`: define de Vite en `vite.config.*` (`__KALY_LIVE_MODEL__: JSON.stringify(process.env.KALY_LIVE_MODEL || 'gemini-2.5-flash-native-audio-preview-09-2025')`) con guard `typeof __KALY_LIVE_MODEL__ !== 'undefined'` en el código (patrón de `__VITE_API_BASE__` en api.js). El backend ya devuelve `model` en la sesión — usarlo con prioridad.

**Test** `gastos-app/tests/gastos/KalyAgent.test.jsx` (fake timers; mock `./live.js` → openLiveSession jest.fn devuelve {sendText, sendToolResponse, close}; mock api.agentSession → {token:'t', model:'m', context:{onboarded:false, saludoHora:'dia', nombre:'', trato:'', resumen:{}}}): (1) primera vez (sin localStorage) → auto-start → openLiveSession llamado y sendText con instrucción de onboarding; (2) con kaly_onboarded=1 y kaly_last_greet=hoy → NO auto-start; tap en la esfera → start manual; (3) silencio: simular onState('listening') → avanzar 5 s → close llamado; (4) onUserTranscript('no gracias') → tras 2.5 s close; (5) toolCall guardar_preferencias → executeTool (mock tools.js) y sendToolResponse.

**Commit:** `feat(kaly): esfera dorada y orquestador con onboarding/saludo/timeouts`

---

## Task 4: GastosApp — 4 botones + 202 + integración K.A.L.Y. (+APK)

**Modify** `gastos-app/src/gastos/GastosApp.jsx`:
- Tabs: `capturar` | `mis` | `transaccional` | `match`. Nav 4 botones: Captura · Movimientos · Transaccional · **Match** (Match con `style={{ color: tab==='match' ? '#fff' : '#b91c1c' }}` y fondo `#b91c1c` cuando activo; los otros patrón actual dorado).
- `Transaccional` placeholder: título "Transaccional", texto "Regístralo sin imagen: díctame o escríbeme el detalle (monto, RUT, folio…) y yo deduzco si es gasto o ingreso, calculo el IVA y lo registro contigo. Próximamente." `Match` placeholder: título "Match" rojo, texto "Conciliación automática: cotejo tus movimientos con el Libro de Compra/Venta del SII y tus cartolas bancarias, incluso pagos masivos (iterando sumas de facturas). Próximamente." Ambos con `<div className="p-6 grid gap-2">`.
- **202 documento:** en `submit()`, tras `const exp = await api.createExpense(...)`: `if (exp && exp.documento) { setMatchDoc(exp.documento); setTab('match'); return; }` (estado nuevo `matchDoc`; el placeholder Match muestra arriba, si `matchDoc`, un aviso: "📄 Detecté {cartola bancaria|libro de compra/venta del SII}. Aquí se activará la conciliación. Próximamente.").
- **K.A.L.Y.:** en el tab capturar, ARRIBA del bloque EvidenceIntake: `<KalyAgent />` (import desde './kaly/KalyAgent.jsx'). Header sigue "Hash IA".
- Vite define `__KALY_LIVE_MODEL__` (ver Task 3) en `gastos-app/vite.config.js` junto al define existente; y en `gastos-app/jest.config.*`/setup global si hace falta (globals para jest: definir en `tests/setup` `global.__KALY_LIVE_MODEL__ = 'test-model'` si el suite lo requiere — seguir el patrón de `__VITE_API_BASE__`).

**Test** actualizar `gastos-app/tests/gastos/GastosApp.test.jsx` (+nuevo bloque): mock `./kaly/KalyAgent.jsx` → `() => <div>kaly-mock</div>` para no abrir sesiones en tests existentes; (1) nav muestra los 4 botones y Match va a placeholder; (2) createExpense resuelve `{ documento: 'cartola' }` → cambia a tab Match y muestra "cartola"; (3) los tests existentes siguen verdes (capture → ConfirmScreen, dup 409). En `GastosApp-dup.test.jsx` agregar el mismo mock de KalyAgent.

**Steps finales:** FULL suite app verde → commit `feat(kaly): barra 4 botones, match 202 y esfera en captura` → `npm run build && npx cap sync android && gradlew assembleDebug` (cambiar `versionName`/output a v1.4 si trivial; si no, dejar) → NO subir aún (runbook con el usuario: migrar VPS + redeploy backend + subir APK + probar voz real + validar ephemeral tokens con la key).

---

## Self-Review
- Spec B1-B7 cubiertos: B1 live.js (T2), B2 orb (T3), B3 tools+confirmación-en-prompt (T1/T2), B4 prompt fiel (T1), B5 orquestador timers (T3), B6 detección → ya backend; app 202 (T4), B7 tabs (T4). Riesgo ephemeral tokens se valida en runbook.
- Consistencia: `openLiveSession` contrato usado igual en T2 (def) y T3 (uso); `executeTool(name,args,{onPrefsSaved})` T2 def / T3 uso; `api.agentSession()` devuelve `{token, model, expireAt, context}` (backend F1-A) — KalyAgent usa `s.model` con fallback define. `decideAutoStart` usa localStorage keys `kaly_onboarded`/`kaly_last_greet` (T1 def como params puros; T3 los lee).
- Placeholder scan: el detalle de audio (startMic/createPlayer) está especificado por contrato y formato exacto (PCM16 16k/24k, base64, RMS, flush/onDrain) — el implementador escribe el cuerpo; aceptado porque es código de navegador no testeable en jsdom y el contrato está completo.
